# Vite development proxy target (no browser CORS setup needed):
API_PROXY_TARGET=http://127.0.0.1:8000

# Optional direct API endpoint. Set at build time for deployment:
# VITE_API_URL=https://api.example.com
